<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet" 
    integrity="sha384-rbsA2VBKQhggwzxH7pPCaAqO46MgnOM80zW1RWuH61DGLwZJEdK2Kadq2F9CUG65" crossorigin="anonymous">
    <title>blog</title>
</head>
<body>
  <div class="container">
    <h1 class="mb-4">BLOG ARTICLE</h1>
    <a href="/article/new" class="btn btn-success">New Article</a>

    <!-- <% article.forEach(a => { %>
        <div class="card mt-4">
            <div class="card-body">
                <h4 class="card- title"><%= a.title %></h4>
                <div class="card-date mb-2"><%= a.createdAt.toLocaleDateString() %></div>
                <div class="card-description"><%= a.description %></div>
            </div>
        </div>
   <% })%> -->

   <% for(const a of article){%>

        <div class="card mt-4">
            <div class="card-body">
                <h4 class="card- title"><%= a.title %></h4>
                <div class="card-date mb-2"><%= a.createdAt.toLocaleDateString() %></div>
                <div class="card-description"><%= a.description %></div>
                <a href="articles/edit/<%= article.id %>" 
                class="btn btn-info">Edit</a>
                <form action="/article/<%= article.id %>?_method_DELETE"
                method="POST" class="d-inline" >
                <button type="submit" class="btn btn-danger">Delete</button>
            </form>
            </div>
        </div>

    <%}%>


  </div>
</body>
</html>